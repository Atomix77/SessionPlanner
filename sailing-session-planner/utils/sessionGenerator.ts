export function generateSessionPlan(sessionInfo) {
    const {
        instructorCount,
        studentCount,
        ageRange,
        sessionLength,
        courseType,
        course,
        windSpeed,
        gustSpeed,
        tideStrength,
        tideDirection,
        waveHeight,
        tidal,
        selectedBoats,
        games,
    } = sessionInfo;
    
    return [];
}
